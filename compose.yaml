# This project no longer runs a standalone Postgres container. The DB lives
# inside the Supabase local stack — start it with `pnpm db:start` (or
# `supabase start`) and stop it with `pnpm db:stop` (or `supabase stop`).
#
# See README.md and supabase/config.toml for details.
